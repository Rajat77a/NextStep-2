# Development Setup

Basic local setup:

1. Install dependencies with `npm install`.
2. Start the dev server with `npm run dev`.
3. Open the local Vite URL in a browser.
4. Use seeded or locally created data to explore the portals.

The app is TypeScript-based, so build checks may catch issues that do not appear during dev server usage.

