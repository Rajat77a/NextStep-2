# Dependency Notes

The project uses Vite, React, TypeScript, Tailwind CSS, Radix UI components, React Router, and related UI libraries.

Dependency care points:

- Keep the lockfile committed.
- Use the expected Node and npm versions for deployment.
- Avoid registry mirror URLs in the committed lockfile.
- Run a clean install when debugging Vercel install failures.

