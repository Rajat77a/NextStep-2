# Manual QA Notes

Manual QA should focus on the paths real users are most likely to touch.

Suggested smoke tests:

- Landing page loads.
- Login and signup screens render.
- Parent dashboard opens for a parent user.
- Teacher dashboard opens for a teacher user.
- Admin dashboard opens for an admin user.
- Unknown routes return to the app instead of a host 404.

