# Repository Map

Important project areas:

- `src/pages` contains route-level screens.
- `src/components` contains shared and UI components.
- `src/api` contains local data and auth helpers.
- `src/hooks` contains reusable React hooks.
- `src/types` contains shared TypeScript types.
- `vercel.json` contains the single-page app rewrite.

