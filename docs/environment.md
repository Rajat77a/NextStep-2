# Environment Notes

The current project does not require backend environment variables for normal local use.

Useful assumptions:

- The app runs in the browser.
- Data is stored locally.
- No production database is configured.
- No server-side API secrets should be placed in client code.

If a backend is added later, environment variable documentation should be updated before deployment.

