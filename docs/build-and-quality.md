# Build And Quality Checks

Recommended checks before deployment:

- `npm install`
- `npm run build`
- Manual route smoke test
- Login/signup smoke test
- Parent portal smoke test
- Teacher portal smoke test
- Admin portal smoke test

The build script runs TypeScript project checks before Vite creates the production bundle.

